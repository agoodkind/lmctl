name: CI
on: [push, pull_request]

permissions:
  contents: read
  attestations: write

jobs:
  ci:
    uses: agoodkind/go-makefile/.github/workflows/_ci.yml@main
    permissions:
      contents: read
      id-token: write
      attestations: write
    secrets: inherit
